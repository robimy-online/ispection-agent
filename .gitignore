node_modules/
dist/
*.log
# local agent state (private key, buffer, meta) — NEVER commit
.ispection-agent/
data/
# local secrets (your filled-in copy of .env.example) — .env.example itself stays tracked
.env
.DS_Store
.claude