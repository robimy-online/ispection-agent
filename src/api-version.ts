// API contract version this agent speaks (X-Api-Version header); the collector rejects versions that are too old.
// Wire-contract copy — must match the ispection server's API version; bump both when the contract changes.
export const INGEST_API_VERSION = 1;
